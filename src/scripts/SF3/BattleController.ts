/**
 * SF3/BattleController.ts
 * Mirror of SF3/BattleController.cs
 *
 * Was an empty stub — now re-exports from the root BattleController
 * so any import from either path works without duplication.
 */

export { BattleController } from "../BattleController";
