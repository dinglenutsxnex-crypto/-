// Ported from Assembly-CSharp/SF3/Moves/AttackType.cs
// Original used `enum AttackType : byte` — TS enums are numeric by default.

export enum AttackType {
  UnarmedDamage = 0,
  WeaponDamage  = 1,
  RangeDamage   = 2,
}
